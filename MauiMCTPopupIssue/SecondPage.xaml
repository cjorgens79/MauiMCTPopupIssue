<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             x:Class="MauiMCTPopupIssue.SecondPage">

    <Grid>
        <!-- Enclose SKCanvasView in ITS OWN single-cell Grid. Otherwise, iOS won't get the touch events. -->
        <Grid x:Name="gridSkiaContainer">
            <skia:SKGLView x:Name="glView" HasRenderLoop="True" PaintSurface="glView_PaintSurface" />
        </Grid>

        <VerticalStackLayout Padding="30,0" Spacing="25" Background="Transparent">
            <Image Source="dotnet_bot.png" HeightRequest="185" Aspect="AspectFit" />

            <Label Text="Hello, World!" Style="{StaticResource Headline}" SemanticProperties.HeadingLevel="Level1" />

            <Label Text="Welcome to &#10;.NET Multi-platform App UI" Style="{StaticResource SubHeadline}" SemanticProperties.HeadingLevel="Level2" SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button x:Name="CounterBtn" Text="Close"  Clicked="OnClose_Clicked" HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </Grid>

</ContentPage>
